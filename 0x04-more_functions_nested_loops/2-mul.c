#include "main.h"
#include <stdio.h>

/**
 * mul - to multiply two digits
 * @a: contains the first given digit.
 * @b: contains the second given digit.
 * Return: returns the multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
