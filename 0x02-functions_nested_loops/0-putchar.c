#include <stdio.h>
#include "main.h"

/**
 * main - the main func of the project
 * Description: the entry point of the file
 * Return: return 0 and close
 */
int main(void)
{
	char* msg;
	msg = "_puchar";
	_putchar(msg);
	return (0);
}
